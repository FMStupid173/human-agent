# 人味 Agent 30-Second Promo

This is the editable Remotion source for the 1080 x 1920 launch video.

```powershell
npm install
npm run render
npm run still
```

The rendered MP4 is intentionally silent so the final post can use music licensed inside Douyin or Xiaohongshu. The on-screen copy carries the complete argument without audio.
